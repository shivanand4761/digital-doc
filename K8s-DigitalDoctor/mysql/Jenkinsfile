pipeline {
    agent any

    environment {
        BRANCH_NAME = "${env.BRANCH_NAME}"
        ENV = "${env.BRANCH_NAME.toLowerCase()}"
        APP_NAME = "mysql"
    }
    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }   
        stage('Deploy to Kubernetes') {
            steps {
                dir('K8s-DigitalDoctor/mysql') {
                    sh '''
                        kubectl apply -f K8s-DigitalDoctor/mysql/mysql-deployment.yaml -n digiDoc-Dev
                        kubectl apply -f K8s-DigitalDoctor/mysql/mysql-svc.yaml -n digiDoc-Dev
                    '''
                }
            }
        }
    }

    post {
        success {
            echo "✅ Deployed $APP_NAME successfully!"
        }
        failure {
            echo "❌ Deployment of $APP_NAME to $ENV failed."
        }
    }
}