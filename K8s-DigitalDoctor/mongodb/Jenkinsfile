pipeline {
    agent any

    environment {
        BRANCH_NAME = "${env.BRANCH_NAME}"
        ENV = "${env.BRANCH_NAME.toLowerCase()}"
        APP_NAME = "mongodb"
    }
    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }   
        stage('Deploy to Kubernetes') {
            steps {
                dir('K8s-DigitalDoctor/mongodb') {
                    sh '''
                        kubectl apply -f K8s-DigitalDoctor/mongodb/mongodb-deployment.yaml -n digiDoc-Dev
                        kubectl apply -f K8s-DigitalDoctor/mongodb/mongodb-svc.yaml -n digiDoc-Dev
                    '''
                }
            }
        }
    }

    post {
        success {
            echo "✅ Deployed $APP_NAME successfully to $ENV!"
        }
        failure {
            echo "❌ Deployment of $APP_NAME to $ENV failed."
        }
    }
}