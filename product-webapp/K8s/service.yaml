apiVersion: v1
kind: service
metadata:
  name: product-webapp
spec:
  selector:
    app: product-webapp
  ports:
  - protocol: TCP
    port: 3000
    targetPort: 3000
    nordPort: 30002
  type: NordPort